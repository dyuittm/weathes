<div class="container">
  <div class="row">
    <div class="col-md-12 mx-auto mt-3">
      <h3 class="text-center">-favorite-</h3>
      <% if @favorite_posts.present? %>
        <%= render "customer/posts/index", posts: @favorite_posts %>
      <% else %>
        <p>お気に入りの記事はありません</p>
      <% end %>
      <%= paginate @favorite_posts %>
    </div>
  </div>
</div>
