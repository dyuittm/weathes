<%= form_with(model:[post, post_comment], local: false) do |f| %>
  <%= f.text_area :comment, rows:'1',placeholder: "comment", class: "w-100" %><br>
  <%= f.button :type => "submit", class: "btn btn-sm btn-outline-dark mt-20 pull-right float-right" do %>
    <i class="far fa-comment-dots"></i>comment
  <% end %>
<% end %>