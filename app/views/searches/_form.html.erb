<%= form_with url: search_path, local: true, method: :get, class: "search-container d-flex justify-content-end" do |f| %>
  <%= f.select :model, options_for_select({ "user" => "customer", "post" => "post", "prefecture" => "prefecture" }) %>
  <div class="search-box">
    <%= f.text_field :content, placeholder: "search", style: "border: none" %>
    <%= f.button :type => "submit", style: "border: none" do %>
      <i class="fa-solid fa-magnifying-glass"></i>
    <% end %>
  </div>
<% end %>