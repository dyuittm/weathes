<%= form_with(model:[post, post_comment], local: false) do |f| %>
  <%= f.text_area :comment, rows:'2',placeholder: "コメント", class: "w-100" %><br>
  <%= f.button :type => "submit", class: "btn btn-outline-dark mt-20 pull-right float-right" do %>
    <i class="fa-regular fa-paper-plane"></i>comment
  <% end %>
<% end %>